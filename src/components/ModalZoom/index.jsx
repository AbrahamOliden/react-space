function ModalZoom({ photo }) {
    return <>
        <dialog open={!!photo}>
            <p>Cierrame we</p>
            <form method="dialog">
                <button>Weno</button>
            </form>
        </dialog>
    </>
};

export default ModalZoom;